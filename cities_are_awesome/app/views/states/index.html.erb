<h1>States</h1>

<% @states.each do |state| %>

  <h2><%= state.name %></h2>

  <%= state.cities.map{|city| city.name}.join(",") %>

  <%= state.cities.map(&:name).join(",") %>

  <%= state.cities.pluck(:name).join(",") %>

  <%= state.city_names %>



<% end %>
